const fs = require('fs');
const path = require('path');

const dir = 'c:\\AQS-PULKITGUPTA025\\node_modules';
console.log("fs.existsSync(dir):", fs.existsSync(dir));
if (fs.existsSync(dir)) {
  const files = fs.readdirSync(dir);
  console.log("Files in node_modules:", files.slice(0, 10));
} else {
  console.log("node_modules does not exist at all.");
}
